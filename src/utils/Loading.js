import React from "react";
// import CircularProgressWithLabel from "mui/uri";
import { CircularProgress } from "@mui/material";

const Loading = () => {
  return (
    <div>
      <CircularProgress color="success" />
    </div>
  );
};

export default Loading;
